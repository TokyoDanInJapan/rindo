/// Uniform gap between the screen edges and the floating map chrome: the frame
/// controls, the banners, the compass and the legend. The value 16 matches the
/// margin Material fixes for the Scaffold's floating action buttons,
/// kFloatingActionButtonMargin. That margin cannot be changed without a custom
/// location, so everything else aligns to it.
const screenMargin = 16.0;
