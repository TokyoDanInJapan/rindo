/// Uniform gap between the screen edges and the floating map chrome (frame
/// controls, banners, compass, legend). 16 matches the margin Material fixes
/// for the Scaffold's floating action buttons (kFloatingActionButtonMargin),
/// which can't be changed without a custom location - so everything else
/// aligns to it.
const screenMargin = 16.0;
